# CONTRACT — {slug}

The project lens. Composed into every {slug} agent's hat (`charter + role + this`).
Identity is **static**; the roadmap shifts on **milestones**; the operational *now* is
**read fresh**, not baked here.

> Auto-scaffolded by `rv project new` on {date}. Fill every FILL placeholder below before the
> first crew dispatch, then run `rv build-agents --project {slug}` to re-bake the hats.

## Identity

**What it is.** <!-- FILL: one-paragraph description of this project — what it produces, for whom. -->

**Profile.** <!-- FILL: e.g. code (SWE) + research-methodology. -->

**Where it lives.** Repo `{source_dir}` (code `{code}`).

**Conventions / golden rules** (verify against the repo, not memory):
- <!-- FILL: the standing rules a fresh agent must not violate — one bullet each. -->

**Care.** <!-- FILL: the irreversible / destructive / outward-facing edges that need an explicit nod. -->

## ★ POINTERS — know where things live

- **Design-of-record** — <!-- FILL: path to the authoritative design/task doc. -->
- **Architecture** — `{source_dir}/architecture.md` (the component/data-flow map).
- **Control bus** — `rv status --project {slug}` (never eyeball the raw control file).
- <!-- FILL: skills, registries, results source, any other load-bearing pointer. -->

## Roadmap

<!-- FILL: current phase + the milestone sequence. Slow-moving strategic content only —
     operational state stays out (read fresh from the board). -->

## Your team (roster)

{slug}'s crew: {roster}. The hub is the sole spawner; you **request** convening, you don't spawn.

### By role — the lens each hat leans on

<!-- FILL (one line per role in the roster): role → the pointer/doctrine it leans on. -->

## Operational state — read fresh, not baked here

For what's on the plate *right now*: `rv status --project {slug}` and the task board.
This contract is the *strategic* lens; the board and CONTROL are the *operational* now.
