"""manuscript — SR-MS-1a: manuscript structure for Research Vault.

The manuscript OKF type is a LaTeX-native POINTER note: metadata + provenance
that points to the LaTeX artifacts in manuscripts/<id>/. Prose lives in .tex;
the note records lineage.

This package provides:
  - style.py: the per_section_tips config seam (adopter-customizable per venue)
"""
